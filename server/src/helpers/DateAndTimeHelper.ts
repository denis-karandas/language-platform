export const convertHoursToMilliseconds = (value: number): number => {
  return value * 60 * 1000;
};
export const convertDaysToMilliseconds = (value: number): number => {
  return value * 24 * 60 * 60 * 1000;
};
